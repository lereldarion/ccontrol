SUBDIRS = src

# pkgconfigdir set by PKG_INSTALLDIR in configure.ac
pkgconfig_DATA = ccontrol.pc

# udevrulesdir set by configure.ac (got from pkgconfig too)
udevrules_DATA = 99-ccontrol.rules

doc_DATA = README.md AUTHORS

# TODO : manage distribution (problem with kernel module integration)
